Summary of work:

1. Read CSV file and created a dataframe with the pandas library.
2. Scatter plot comparing features with the output parameter using the matplotlib.pyplot library
3. Compared all features of the dataset among each other using the seaborn library
4. plotted a correlation matrix that relates all features in the dataset, to explore the relationship among the features using the dataframe method and seaborn.
5. Linear regression model import from Sklearn, split dataset for training and testing, and fit model.
6. Determination of the model accuracy (R-squared) for both the training and testing dataset (0.584 & 0.4578)
7. Applied features scaling/engineering to scale features values withing the range of 3 and -3, using  StandardScaler from the sklearn library
8. Re-train/fit model using the scaled features value dataset.
6. Determination of the model accuracy (R-squared) for both the training and testing dataset (0.584 & 0.4578)
7. Tested out the model with new data.
8. plotted the actual and predicted values of the model for both the training and testing dataset, with the perfect fit line and error bands +20%, +10%, -10%, -20%
